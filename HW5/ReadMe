#To access PyTorch and Python 3.6+ in scholar cluster:

module load learning/conda-5.1.0-py36-gpu

#And then (I use 1.4.0)

module load ml-toolkit-gpu/pytorch/1.4.0


Q3: Domain adaptation

Run the code without using MMD, through the following command 

python hw5.py --reg_str 0

Run the code with MMD, through the command 

python hw5.py --reg_str 10 --kernel_sigma 20